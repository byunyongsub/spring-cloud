server:
  port: 8081

spring:
  application:
    name: auth-server
  datasource:
    hikari.maximum-pool-size: 4
    url: jdbc:postgresql://localhost:5432/test
    username: test
    password: qwe123
  jpa.hibernate.ddl-auto: update

security:
  oauth2:
    jwt:
      signkey: 123@#$