server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    discovery:
      enabled: true
    config:
      server:
        native:
          searchLocations: classpath:/config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/